{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.edu/contracts/equipment-status-changed.payload.schema.json",
  "title": "EquipmentStatusChanged payload",
  "type": "object",
  "required": [
    "orderId",
    "equipmentId",
    "targetStatus",
    "operatorId",
    "reason"
  ],
  "properties": {
    "orderId": {
      "type": "string",
      "pattern": "^WO-[0-9]{8}-[0-9]{4}$"
    },
    "equipmentId": {
      "type": "string",
      "pattern": "^EQ-[0-9]{6}$"
    },
    "targetStatus": {
      "type": "string",
      "enum": [
        "RUNNING",
        "WARNING",
        "STOPPED",
        "MAINTAINING",
        "TRIAL_RUNNING"
      ]
    },
    "operatorId": {
      "type": "string",
      "pattern": "^USER-[A-Z0-9-]{1,27}$"
    },
    "reason": {
      "type": "string",
      "minLength": 1,
      "maxLength": 300
    }
  },
  "additionalProperties": false
}
